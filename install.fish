mkdir -p ~/.setup
cd ~/.setup
git clone https://github.com/tbjgolden/unix-dev-setup.git
cd ~/.setup/unix-dev-setup

echo "Step 1) Run `fish` to open a fish shell."
echo "Step 2) Run `fish ~/.setup/unix-dev-setup/sync.fish` to start the install"
